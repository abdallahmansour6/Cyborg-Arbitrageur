**Context & Background:**
I am building a high-frequency, cross-exchange crypto perp-perp arbitrage "Execution Engine" in Python using CCXT Pro. It is an always-on, stateful async daemon that executes a "Synchronized Smart Slicing" algorithm to capture funding rate anomalies via dual-leg, delta-neutral IOC limit orders.

You have access to the current Execution Engine codebase in my workspace.

Previously, I built a separate project (a Market Scanner) to find these anomalies. While building the Scanner, I realized that crypto exchange APIs are chaotic: documentation lies, CCXT abstractions leak, and exchanges use vastly different conventions and have idiosyncrasies. To solve this, I instituted a strict **"Probing Methodology and Field Notes"** architecture. I built standalone scripts to empirically test live API endpoints and documented the verified truths in a living markdown file. The Scanner's core logic *only* relies on these verified truths.

You can check the Scanner's `FIELD_NOTES.md` @../Arb-Scanalytics/FIELD_NOTES.md and `probes.py` @../Arb-Scanalytics/probes.py as inspiration so you can see the rigor and format of this approach.

**Recent Architectural Context (2026-05-08):**
The codebase just landed an asymmetric-symbols refactor that introduced `primitives.py` (`ExecutionLeg` / `ExecutionPair` / `parse_base`). Per-leg multiplier and `contract_size` knowledge is now centralized in these primitives; engine and execution code consume them via `to_native_qty` / `to_base_qty` / `to_base_price`. The warmup handler already emits a `LEG FINGERPRINT` log line per leg — a one-shot dump of (base_coin, multiplier, contract_size, per-1×-base price) intended as a human-eyeballable sanity gate before any entry. **This is the seed pattern for engine-side runtime probing.** The new probing system should integrate with and extend it (more runtime probes triggered at warmup or on demand), not parallel-implement it.

**The Goal:**
The Execution Engine was built before I adopted this empirical methodology. It currently relies on dangerous "clean world" assumptions (e.g., assuming CCXT class names map perfectly, assuming WebSocket streams never silently die, assuming order-create responses always carry fill data).

I want to establish a bespoke **Probing Methodology and Field Notes ecosystem specifically for the Execution Engine.**

**Your Task:**
Please act as a senior architectural engineer. You will own the probe → field-notes → implementation cycle end to end. Pause and ask me when you hit something only the operator can do — funding a venue's perp account, rotating API-key scopes or permissions, per-run authorization for capital-at-risk probes, or any other prerequisite on the exchange interface — and I'll handle it on my side before you continue. Do not just blindly port the Scanner's logic. The Engine is about *execution* and *state*, not just data gathering. I want you to explore and design a probing architecture tailored to the Engine's unique kinetic physics.

Please provide a strategic roadmap and initial implementations for the following:

1.  **Execution-Focused Probes:** Design a probing architecture (e.g., `engine_probes.py`) to empirically validate our execution assumptions. Provide initial implementations for **2–3 representative probes covering different safety classes** (introspection, safe-by-construction orders, capital-at-risk one-offs, continuous watchdogs), with the rest as roadmap stubs in `ENGINE_FIELD_NOTES.md`. *To give you an idea of the caliber I am looking for, here are some execution physics I suspect we need to probe:*
    *   **IOC & reduceOnly Honor:** How do we verify an exchange *actually* cancels an IOC instead of resting it? (Perhaps safe-by-construction, sub-min-lot orders far from the spread?)
    *   **Receipt Shapes:** Verifying CCXT receipts actually populate fill fields (`average`, `cost`+`filled`) so our `_fill_vwap()` math doesn't return 0.0. Equally critical — and orthogonal — is **whether the placement response is fill-resolved at all on each venue**, or whether the venue returns only an `orderId` and requires a follow-up `fetch_order` to read the settled IOC outcome. A venue that silently returns all-None fill fields on placement will look like a zero-fill to naive receipt accounting and let asymmetric exposure accumulate cycle after cycle without any state update — the engine's books stay clean while real venue-side positions diverge.
    *   **Unit Conventions:** Empirically testing `contractSize` math against base quantities.
    *   **L2 Delivery:** Bounding the time to first snapshot delivery.
    *   *What else should we be probing to make execution bulletproof? Expand on this.*

2.  **Engine Field Notes:** Draft the structure for an `ENGINE_FIELD_NOTES.md` document. Based on your review of the current Engine codebase and CCXT execution quirks, what categories and tables/maps need to be tracked here to ensure our slicing loop and imbalance recovery never encounter a surprise in production?

3.  **Vulnerability Identification:** Review the current Engine codebase (which you have access to). Point out the top architectural areas where the Engine is currently making dangerous assumptions that should be replaced by our new empirical probing methodology. How should we refactor the Engine's core abstractions (like how it handles **execution receipts** (resolution + fill-state correctness), **WebSocket stream liveness** (silent staleness detection), or **order book sufficiency** (depth / spread / age sanity at slice time)) to align with this new rigorous mindset?

Be innovative. Think outside the box. I am looking for architectural primitives that make the Engine bulletproof, lean, and highly fault-tolerant based on empirical reality, not superficial assumptions.